<%# .update_all is useful if you have multiple tweets. There might be a case
where you have retweets of your tweets in view and they all get liked
at the same time by you, then you want to update each accordingly %>
<%= turbo_stream.update_all(".#{dom_id(@likeable)}_likes") do %>
  <%= render "likes/like", likeable: @likeable %>
<% end %>

<%# Getting the like size on the page %>
<%= turbo_stream.update_all(".#{dom_id(@likeable)}_likes-size", @likeable.likes.size) %>
