<div class="px-6 py-3" id="<%= dom_id(tweet) %>">
<%# dom_id is a helper, it will spit out naming conventions for us %>
<%# it looks like this: e.g. tweet_13 (it's the id and the class name lowercased) %>
  <div class="flex space-x-3 items-start pb-3">
    <%= profile_image(tweet.user) %>
  </div>
  <div class="flex-1">
    <p class="font-bold">
      <%= link_to tweet.user.name, profile_path(tweet.user), class: "hover:underline", data: {turbo: false } %>
    </p>
  </div>
  <div class="prose prose-lg">
    <%= tweet.body %>
  </div>

  <%= render "tweets/controls", tweet: tweet %>
  <%# we pass the tweet through as a local variable %>

</div>
